services:
  - name: skillarena-beta-api
    type: web
    runtime: node
    plan: free
    region: ohio
    buildCommand: npm ci && npm run typecheck
    startCommand: npm run server:start
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
